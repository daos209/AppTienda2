import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.apptiend',  // Cambia esto a tu identificador de aplicación
  appName: 'AppTienda',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;